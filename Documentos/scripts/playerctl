#!/bin/bash
if [ "$1" == 'play_pause' ]; then
    if [ -n "$(pgrep -x spotify)" ];
    then
        playerctl --player=spotify play-pause;
    elif [ -n "$(pgrep -x ncspot)" ];then
        if [ "$(playerctl --player=ncspot status)" == "Playing" ]; then
            playerctl --player=ncspot pause;
        elif [ "$(playerctl --player=ncspot status)" == "Paused" ]; then
            playerctl --player=ncspot play;
        fi
    else
        playerctl play-pause;
    fi
elif [ "$1" == 'previous' ]; then
    if [ -n "$(pgrep -x spotify)" ];
    then
        playerctl --player=spotify previous;
    elif [ -n "$(pgrep -x ncspot)" ];then
        playerctl --player=ncspot previous;  
    else
        playerctl previous;
    fi
elif [ "$1" == 'next' ]; then
    if [ -n "$(pgrep -x spotify)" ];
    then
        playerctl --player=spotify next;
    elif [ -n "$(pgrep -x ncspot)" ];then
        playerctl --player=ncspot next;  
    else
        playerctl next;
    fi
fi
